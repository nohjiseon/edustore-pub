export const enum Breakpoint {
  // MINIMUM = 360,
  // S = 480,
  // M = 768,
  // L = 1024,
  // XL = 1200,
  // XXL = 1920,
  // MAXIMUM = 2560
  MOBILE = 1280
}

export const enum ZIndex {
  BACKGROUND = -1,
  DEFAULT = 1,
  TOGGLE = 100,
  HOVER = 200,
  DROPDOWN = 400,
  STICKY = 500,
  DRAWER = 600,
  TOAST = 700,
  OVERLAY = 800,
  MODAL = 900
}

export const DIMMED_ANIMATION_DURATION_MS = 150
