# 백엔드 API 서버 주소 (프록시 대상)
# 서버 사이드에서만 사용되며, 클라이언트에 노출되지 않습니다.
BACKEND_API_URL=http://52.78.34.73

# 클라이언트에서 사용하는 API 기본 URL (프록시 경로)
# 개발/프로덕션 모두 /api 사용 (Next.js가 자동으로 백엔드로 프록시)
NEXT_PUBLIC_API_BASE_URL=/api

# API 디버깅 플래그 (개발 환경에서만 true)
NEXT_PUBLIC_DEBUG_API=false

# 필요 시 추가 환경변수는 여기에 예시로만 기입합니다.
# 실제 비밀키/민감정보는 커밋하지 마세요.
